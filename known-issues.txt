- rescaling browser leaves strange edge artifacts on sciter window sides
- sciter window can be resized inside browser window
- sciter window min-max height prevents changing browser height below sciter window min height
- chatgpt.com crashes the browser
- url in search bar does not update to current page url if user interacts with a link in a website